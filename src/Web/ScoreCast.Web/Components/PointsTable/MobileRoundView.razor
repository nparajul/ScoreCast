@* Vertical list of match cards for mobile bracket view *@
<div style="display:flex;flex-direction:column;gap:8px;padding:12px 0;max-width:320px;margin:0 auto;">
    @foreach (var slot in Slots)
    {
        <MatchCard Slot="slot" />
    }
</div>
